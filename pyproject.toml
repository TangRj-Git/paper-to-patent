[project]
name = "paper-to-patent-skill"
version = "0.3.0"
description = "Codex skill for LaTeX-first staged conversion of academic papers into Chinese invention patent drafting materials."
readme = "README.md"
requires-python = ">=3.10"
dependencies = []

[tool.pytest.ini_options]
testpaths = ["tests"]
